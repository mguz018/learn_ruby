# Copy to .env and fill in. Only the anon key belongs here — never a service role
# key, and never the Anthropic API key (that lives in Supabase Edge Function secrets).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY
