# Cloud sync (optional). Copy to .env.local for local dev, or set these in
# Netlify → Site settings → Environment variables for production.
# Without them, the app runs local-only (no cloud sync) — everything else works.
#
# Find these in your Supabase project: Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
